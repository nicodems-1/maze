[project]
name = "a-maze-ing"
version = "0.1.0"
description = "generate a maze, solve it and display it"
authors = [
    {name = "chafonta, niverdie"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=1.20.2",
]
